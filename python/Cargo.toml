[package]
name = "cast-value-rs"
version = "0.1.0"
edition = "2021"
license = "MIT OR Apache-2.0"

[lib]
name = "_cast_value_rs"
crate-type = ["cdylib"]

[dependencies]
zarr-cast-value-core = { path = "../core" }
pyo3 = { version = "0.25", features = ["extension-module"] }
numpy = "0.25"
